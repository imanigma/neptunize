[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/health.json"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
startCommand = "node_modules/.bin/serve dist -s -p $PORT"

[env]
NODE_ENV = "production"

# Optimized for Railway frontend deployment
