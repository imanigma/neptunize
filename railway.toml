[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/health.json"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
startCommand = "node server.js"

[env]
NODE_ENV = "production"

# Optimized for Railway frontend deployment
