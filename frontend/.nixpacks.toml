[variables]
NPM_CONFIG_LOGLEVEL = "error"
NODE_ENV = "production"

[phases.setup]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "echo '{\"status\":\"healthy\"}' > dist/health.json"]

[start]
cmd = "serve dist -s -p $PORT"
