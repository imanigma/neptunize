[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/health.json"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
startCommand = "npx serve dist -s -p $PORT"

# Using Nixpacks with serve package for better Railway compatibility
