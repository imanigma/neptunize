[variables]
PYTHON_VERSION = "3.11"
PIP_CACHE_DIR = "/tmp/pip-cache"

[phases.setup]
cmds = ["pip install --upgrade pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python run.py"
